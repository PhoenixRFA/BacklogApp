const serverUrl = process.env.VUE_APP_BASE_URL;
export const urlBase = serverUrl + '/api/';
export const auth = urlBase + 'auth';
export const projects = urlBase + 'projects';
export const tasks = urlBase + 'tasks';
export const users = urlBase + 'users';
export const resources = urlBase + 'resources';
